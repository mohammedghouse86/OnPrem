node_modules/
npm-debug.log*
.env
.vscode/

# HAR captures — confidential. They contain live session traffic, credentials
# and internal hostnames. Never commit these.
*.har
*HAR*.json
StarlingX*.json

# Generated credentials reference — holds working session tokens and passwords.
# Matches both apps: ACCOUNTS.txt at the root and second app/ACCOUNTS.txt.
# Regenerate locally with `npm run accounts` and `npm run accounts:second-app`.
ACCOUNTS.txt
